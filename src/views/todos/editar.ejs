<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Tarea</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <style>
        body {
            background-color: #383535; /* Cambia el color de fondo aquí */
            color: #fff; /* Cambia el color del texto si es necesario */
        }
        .card {
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #fff; /* Añade un fondo blanco a las tarjetas si lo prefieres */
            color: #000; /* Color de texto dentro de las tarjetas */
        }
        .card-title {
            font-weight: bold;
        }
        .form-label {
            font-weight: bold;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }
    </style>
</head>
<body>

    <div class="container py-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title mb-4">Editar tarea</h5>
                
                <form action="/api/v1/todospanel/<%= todo.id %>" method="POST">
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="title" class="form-label">Título:</label>
                            <input type="text" id="title" name="title" class="form-control" value="<%= todo.title %>" required>
                        </div>
                        <div class="col-md-3">
                            <div class="form-check mt-4">
                                <input type="checkbox" id="completed" name="completed" class="form-check-input" <%= todo.completed ? 'checked' : '' %>>
                                <label for="completed" class="form-check-label">Completado</label>
                            </div>
                        </div>
                        <div class="col-md-3 d-flex align-items-end">
                            <button type="submit" class="btn btn-primary btn-sm">Guardar</button>
                        </div>
                    </div>
                </form>
                
            </div>
        </div>

        <!-- Image section -->
        <div class="d-flex justify-content-center mt-4">
            <img src="https://www.upm.es/recursosidi/wp-content/uploads/2022/10/132.jpg" alt="Descripción de la imagen" class="img-fluid animate__animated animate__fadeIn">
        </div>

    </div>

    <!-- Bootstrap JS bundle (opcional, para algunos componentes de Bootstrap) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
